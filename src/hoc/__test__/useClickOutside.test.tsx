import React, { useRef } from 'react';
import { render, renderHook } from '@testing-library/react';
import useClickOutside from '../useClickOutside';

describe('useClickOutside()', () => {

    // const ref = {current: {}};
    // const testRef = useRef<HTMLDivElement>(null);
    // const testDiv = TestRenderer.create(<div ref={testRef}></div>);

    // const { result } = renderHook(() => {useClickOutside(testRef, false)});
    it('should change state when click outside', () => {
        
    });

    it('should not change click inside', () => {

    });
});